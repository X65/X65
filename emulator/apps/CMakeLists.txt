fips_ide_group(emulator/x65)

fips_begin_app(x65 windowed)
  fips_files(x65.c)
  if (FIPS_IOS)
    fips_files(ios-info.plist)
  endif()
  fips_deps(roms common)
fips_end_app()

fips_begin_app(x65-ui windowed)
  fips_files(x65.c x65-ui.cc)
  if (FIPS_IOS)
    fips_files(ios-info.plist)
  endif()
  fips_deps(roms common ui)
fips_end_app()
target_compile_definitions(x65-ui PRIVATE CHIPS_USE_UI)
